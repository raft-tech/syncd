apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "syncd-server.fullname" . }}-test-connection"
  labels:
    {{- include "syncd-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "syncd-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
