global:
  postgresql:
    auth:
      username: things
      database: things

image:
  registry: docker.io
  repository: rafttech/syncd-things
  tag: ""

config:
  owner: ""
  updateCount: 10
  waitDuration: 10s

postgresql:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
